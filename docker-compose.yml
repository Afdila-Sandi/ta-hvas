networks:
  hvas-net:
    driver: bridge

services:
  #nginx
  nginx-vue:
    image: nginx:alpine
    container_name: hvas-nginx
    restart: always
    ports:
      - "443:443"
    volumes:
      - ./backend/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./backend/certs:/etc/nginx/certs:ro
      - ./frontend/dist:/usr/share/nginx/html:ro
    networks:
      - hvas-net

  #mqtt
  mosquitto:
    image: eclipse-mosquitto:1.6.15
    container_name: hvas-mqtt
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./backend/mosquitto.conf:/mosquitto/config/mosquitto.conf:ro
      - ./backend/certs:/mosquitto/config/certs:ro
    networks:
      - hvas-net

  #database
  postgres-auth:
    image: postgres:15-alpine
    container_name: hvas-db_auth
    restart: always
    env_file:
      - ./backend/auth-service/.env
    volumes:
      - pgdata_auth:/var/lib/postgresql/data
      - ./backend/auth-service/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - hvas-net

  postgres-telemetry:
    image: postgres:15-alpine
    container_name: hvas-db_telemetry
    restart: always
    env_file:
      - ./backend/telemetry-service/.env
    volumes:
      - pgdata_telemetry:/var/lib/postgresql/data
      - ./backend/telemetry-service/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - hvas-net

  #auth-service
  auth-service:
    build: ./backend/auth-service
    container_name: hvas-auth
    restart: always
    env_file:
      - ./backend/auth-service/.env
    depends_on:
      - postgres-auth
    networks:
      - hvas-net

  #telemetry-service
  telemetry-service:
    build: ./backend/telemetry-service
    container_name: hvas-telemetry
    restart: always
    env_file:
      - ./backend/telemetry-service/.env
    depends_on:
      - postgres-telemetry
      - mosquitto
    networks:
      - hvas-net

  #control-service
  control-service:
    build: ./backend/control-service
    container_name: hvas-control
    restart: always
    env_file:
      - ./backend/control-service/.env
    depends_on:
      - mosquitto
    networks:
      - hvas-net

  #api-gateway
  api-gateway:
    build: ./backend/api-gateway
    container_name: hvas-api-gateway
    restart: always
    expose:
      - "5000"
    depends_on:
      - auth-service
      - telemetry-service
      - control-service
    networks:
      - hvas-net

volumes:
  pgdata_auth:
  pgdata_telemetry:
